services:
  second-eye:
    build:
      context: .
      args:
        HTTP_PROXY: ${PROXY:-}
        HTTPS_PROXY: ${PROXY:-}
    image: second-eye-jev:local
    container_name: second-eye-jev
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "18001:8000"
      - "16081:6080"
    environment:
      DATABASE_URL: sqlite:////app/data/goodprice.db
      PROXY: ${PROXY:-}
      TZ: Asia/Shanghai
    volumes:
      - ./data:/app/data
    shm_size: "512mb"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/stats', timeout=5)"
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  gotify:
    image: gotify/server:latest
    container_name: second-eye-jev-gotify
    restart: unless-stopped
    ports:
      - "18081:80"
    volumes:
      - gotify-jev-data:/app/data

volumes:
  gotify-jev-data:
